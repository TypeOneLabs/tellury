# Build output. The module is named tellury, so `go build` drops the binary here;
# `go build -o tellury-bin` and friends land alongside it.
/tellury
/tellury-*
*.test
*.out
.DS_Store
graphify-out/
tellury-out/

# Local scan settings: names a real account, and may name a profile.
.env
